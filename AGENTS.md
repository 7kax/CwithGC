# CwithGC 协作约定

## TODO 同步

- 迭代过程中发现的、已经由代码检查或测试确认的问题，应及时同步到根目录的 `TODO.md`。
- 优先更新已有的对应条目；没有合适条目时，加入相应模块和优先级。验证限制、工具链警告和 API 契约缺口也应记录，不要只留在交接说明中。
- 只有在完成标准已经满足并有验证依据时才标记 `[x]`；尚未修复、只完成部分验证或依赖后续工作的事项保持 `[ ]`。
- TODO 条目应区分“已完成的修复”“尚未解决的问题”和“当前验证范围”，并在必要时写明复现条件、受影响的配置或暂时的规避方式。
- 更新 TODO 不应掩盖未解决的问题，也不应为了让阶段看起来完成而提前勾选任务。
